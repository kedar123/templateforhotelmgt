<%= form_for(@getrate) do |f| %>
  <% if @getrate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@getrate.errors.count, "error") %> prohibited this getrate from being saved:</h2>

      <ul>
      <% @getrate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
