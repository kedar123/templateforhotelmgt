<%= form_for(@getroomratecodefb) do |f| %>
  <% if @getroomratecodefb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@getroomratecodefb.errors.count, "error") %> prohibited this getroomratecodefb from being saved:</h2>

      <ul>
      <% @getroomratecodefb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
