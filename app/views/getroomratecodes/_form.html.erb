<%= form_for(@getroomratecode) do |f| %>
  <% if @getroomratecode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@getroomratecode.errors.count, "error") %> prohibited this getroomratecode from being saved:</h2>

      <ul>
      <% @getroomratecode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
