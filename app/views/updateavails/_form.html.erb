<%= form_for(@updateavail) do |f| %>
  <% if @updateavail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@updateavail.errors.count, "error") %> prohibited this updateavail from being saved:</h2>

      <ul>
      <% @updateavail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
